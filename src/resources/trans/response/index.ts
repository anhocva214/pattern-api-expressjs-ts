import message_en from './en/message.json'
import message_vi from './vi/message.json'


export const response = {
    en:{
        message: message_en,
    },
    vi: {
        message: message_vi,
    }
}